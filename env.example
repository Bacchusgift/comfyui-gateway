# Optional: Redis URL for persisting task mapping and workers. Leave unset to use in-memory storage.
# No auth:    redis://localhost:6379/0
# With auth:  redis://用户名:密码@host:6379/0   (无用户名时用 redis://:密码@host:6379/0，密码含特殊字符需 URL 编码)
# REDIS_URL=redis://localhost:6379/0

# Optional: Queue cache TTL in seconds (default 5)
# QUEUE_CACHE_TTL_SECONDS=5

# Optional: Worker request timeout in seconds (default 30)
# WORKER_REQUEST_TIMEOUT=30

# Optional: Default Basic Auth for all Workers (e.g. nginx reverse proxy). Leave unset to use per-worker auth only.
# WORKER_AUTH_USERNAME=your_username
# WORKER_AUTH_PASSWORD=your_password

# Optional: MySQL for persistence (workers, task mapping, queue, settings). When MYSQL_DATABASE is set, Redis is optional for these.
# Create database and run: mysql -h HOST -u USER -p DATABASE < docs/mysql_schema.sql
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=gateway
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=comfyui_gateway

# Authentication settings for admin panel and API access
# Admin credentials for logging into the management interface
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# JWT secret for generating auth tokens (change this in production!)
JWT_SECRET=your-secret-key-change-in-production

# JWT token expiration time in hours (default: 24)
# JWT_EXPIRE_HOURS=24
